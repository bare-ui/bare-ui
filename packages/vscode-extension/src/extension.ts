import type { ExtensionContext } from 'vscode'

// Entry point for the Wire UI VS Code extension.
//
// Scaffold only — feature wiring (data-* autocomplete, hover docs, snippets,
// diagnostics, commands) lands in subsequent 0.8 tasks. See .claude/roadmap.md.

export function activate(_context: ExtensionContext): void {
  // No-op for now.
}

export function deactivate(): void {
  // No-op for now.
}
