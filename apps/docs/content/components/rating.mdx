---
title: Rating
description: Star rating with hover preview, read-only, and disabled modes.
---

import { Tabs } from 'nextra/components'


# Rating

Star rating component. Hover preview shows the prospective rating before clicking. Supports read-only (non-interactive) and disabled modes.

## Features

<FeatureList>
  <FeatureItem>Hover preview shows prospective rating before clicking.</FeatureItem>
  <FeatureItem>Supports read-only and disabled modes.</FeatureItem>
  <FeatureItem>Can be controlled or uncontrolled.</FeatureItem>
  <FeatureItem>Custom star styling via starClassName prop.</FeatureItem>
</FeatureList>

### Example

<Preview><RatingPreview /></Preview>

<Tabs items={['Anatomy', 'Styled (Tailwind)']}>
  <Tabs.Tab>
    <ForReact>
    ```tsx
import { Rating } from '@wire-ui/react'

<Rating value={4} readOnly />
    ```
    </ForReact>
    <ForVue>
    ```vue
<script setup>
import { Rating } from '@wire-ui/vue'
</script>

<template>
    <Rating value={4} readOnly />
</template>
    ```
    </ForVue>
    <ForSolid>
    ```tsx
import { Rating } from '@wire-ui/solid'

<Rating value={4} readOnly />
    ```
    </ForSolid>
  </Tabs.Tab>
  <Tabs.Tab>
    <ForReact>
```tsx
import { Rating } from '@wire-ui/react'

<div className="flex items-center gap-2">
  <Rating value={4} readOnly className="flex gap-0.5" starClassName="size-4 cursor-default text-[#e5e5e5] data-[filled]:text-black" />
  <span className="text-sm font-medium text-black">4.0</span>
  <span className="text-sm text-[#6b7280]">(128 reviews)</span>
</div>
```
</ForReact>
<ForVue>
```vue
<script setup>
import { Rating } from '@wire-ui/vue'
</script>

<template>
  <div class="flex items-center gap-2">
    <Rating :value="4" read-only class="flex gap-0.5" star-class-name="size-4 cursor-default text-[#e5e5e5] data-[filled]:text-black" />
    <span class="text-sm font-medium text-black">4.0</span>
    <span class="text-sm text-[#6b7280]">(128 reviews)</span>
  </div>
</template>
```
</ForVue>
    <ForSolid>
```tsx
import { Rating } from '@wire-ui/solid'

<div class="flex items-center gap-2">
  <Rating value={4} readOnly class="flex gap-0.5" starClassName="size-4 cursor-default text-[#e5e5e5] data-[filled]:text-black" />
  <span class="text-sm font-medium text-black">4.0</span>
  <span class="text-sm text-[#6b7280]">(128 reviews)</span>
</div>
```
    </ForSolid>
  </Tabs.Tab>
</Tabs>

## Styling

Rating stars expose `data-filled` (at or below current value) and `data-highlighted` (hover preview). Style both via `starClassName`.

```tsx
<Rating
  starClassName="
    text-gray-300
    data-[filled]:text-black
    data-[highlighted]:text-black
    data-[disabled]:opacity-50
    hover:scale-110
  "
/>
```

## Using data attributes

Each star element sets `data-filled` and `data-highlighted` independently. `data-disabled` is set when the Rating is disabled.

```css
/* Unfilled star */
.star { color: #e5e5e5; }

/* Filled or hovered */
.star[data-filled],
.star[data-highlighted] { color: #000; }
```

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `value` | `number` | — | Controlled rating value |
| `defaultValue` | `number` | `0` | Initial value for uncontrolled use |
| `onChange` | `(value: number) => void` | — | Called when rating changes |
| `max` | `number` | `5` | Total number of stars |
| `readOnly` | `boolean` | `false` | Non-interactive; renders `role="img"` |
| `disabled` | `boolean` | `false` | Disables interaction |

## Star data attributes

| Attribute | When present |
|---|---|
| `data-filled` | This star is at or below the current rating |
| `data-hover` | Mouse is hovering this star (hover preview) |
