---
title: Divider
description: Horizontal or vertical separator line.
---

import { Tabs } from 'nextra/components'


# Divider

Horizontal or vertical separator. Decorative by default (`aria-hidden`). Pass `decorative={false}` for a semantic separator.

## Features

<FeatureList>
  <FeatureItem>Supports horizontal and vertical orientation.</FeatureItem>
  <FeatureItem>Decorative by default with aria-hidden.</FeatureItem>
  <FeatureItem>Semantic separator mode with role="separator".</FeatureItem>
</FeatureList>

### Example

<Preview><DividerPreview /></Preview>

<Tabs items={['Anatomy', 'Styled (Tailwind)']}>
  <Tabs.Tab>
    <ForReact>
    ```tsx
import { Divider } from '@wire-ui/react'

{/* Horizontal */}
<div>
  <p>Item one</p>
  <Divider />
  <p>Item two</p>
  <Divider />
  <p>Item three</p>
</div>

{/* Vertical */}
<div>
  <span>Home</span>
  <Divider orientation="vertical" />
  <span>About</span>
  <Divider orientation="vertical" />
  <span>Contact</span>
</div>

{/* With label */}
<div>
  <Divider />
  <span>OR</span>
  <Divider />
</div>
    ```
    </ForReact>
    <ForVue>
    ```vue
<script setup>
import { Divider } from '@wire-ui/vue'
</script>

<template>
    <!-- Horizontal -->
    <div>
      <p>Item one</p>
      <Divider />
      <p>Item two</p>
      <Divider />
      <p>Item three</p>
    </div>

    <!-- Vertical -->
    <div>
      <span>Home</span>
      <Divider orientation="vertical" />
      <span>About</span>
      <Divider orientation="vertical" />
      <span>Contact</span>
    </div>

    <!-- With label -->
    <div>
      <Divider />
      <span>OR</span>
      <Divider />
    </div>
</template>
    ```
    </ForVue>
    <ForSolid>
    ```tsx
import { Divider } from '@wire-ui/solid'

{/* Horizontal */}
<div>
  <p>Item one</p>
  <Divider />
  <p>Item two</p>
  <Divider />
  <p>Item three</p>
</div>

{/* Vertical */}
<div>
  <span>Home</span>
  <Divider orientation="vertical" />
  <span>About</span>
  <Divider orientation="vertical" />
  <span>Contact</span>
</div>

{/* With label */}
<div>
  <Divider />
  <span>OR</span>
  <Divider />
</div>
    ```
    </ForSolid>
  </Tabs.Tab>
  <Tabs.Tab>
    <ForReact>
```tsx
import { Divider } from '@wire-ui/react'

{/* Horizontal */}
<div className="flex flex-col gap-3">
  <p className="text-sm text-black">Item one</p>
  <Divider className="h-[2px] w-full bg-black" />
  <p className="text-sm text-black">Item two</p>
  <Divider className="h-[2px] w-full bg-black" />
  <p className="text-sm text-black">Item three</p>
</div>

{/* Vertical */}
<div className="flex h-6 items-center gap-3">
  <span className="text-sm text-black">Home</span>
  <Divider orientation="vertical" className="h-full w-[2px] bg-black" />
  <span className="text-sm text-black">About</span>
  <Divider orientation="vertical" className="h-full w-[2px] bg-black" />
  <span className="text-sm text-black">Contact</span>
</div>

{/* With label */}
<div className="flex w-64 items-center gap-3">
  <Divider className="h-[2px] flex-1 bg-black" />
  <span className="text-xs font-medium text-[#6b7280]">OR</span>
  <Divider className="h-[2px] flex-1 bg-black" />
</div>
```
</ForReact>
<ForVue>
```vue
<script setup>
import { Divider } from '@wire-ui/vue'
</script>

<template>
  <!-- Horizontal -->
  <div class="flex flex-col gap-3">
    <p class="text-sm text-black">Item one</p>
    <Divider class="h-[2px] w-full bg-black" />
    <p class="text-sm text-black">Item two</p>
    <Divider class="h-[2px] w-full bg-black" />
    <p class="text-sm text-black">Item three</p>
  </div>

  <!-- Vertical -->
  <div class="flex h-6 items-center gap-3">
    <span class="text-sm text-black">Home</span>
    <Divider orientation="vertical" class="h-full w-[2px] bg-black" />
    <span class="text-sm text-black">About</span>
    <Divider orientation="vertical" class="h-full w-[2px] bg-black" />
    <span class="text-sm text-black">Contact</span>
  </div>

  <!-- With label -->
  <div class="flex w-64 items-center gap-3">
    <Divider class="h-[2px] flex-1 bg-black" />
    <span class="text-xs font-medium text-[#6b7280]">OR</span>
    <Divider class="h-[2px] flex-1 bg-black" />
  </div>
</template>
```
</ForVue>
    <ForSolid>
    ```tsx
import { Divider } from '@wire-ui/solid'

{/* Horizontal */}
<div class="flex flex-col gap-3">
  <p class="text-sm text-black">Item one</p>
  <Divider class="h-[2px] w-full bg-black" />
  <p class="text-sm text-black">Item two</p>
  <Divider class="h-[2px] w-full bg-black" />
  <p class="text-sm text-black">Item three</p>
</div>

{/* Vertical */}
<div class="flex h-6 items-center gap-3">
  <span class="text-sm text-black">Home</span>
  <Divider orientation="vertical" class="h-full w-[2px] bg-black" />
  <span class="text-sm text-black">About</span>
  <Divider orientation="vertical" class="h-full w-[2px] bg-black" />
  <span class="text-sm text-black">Contact</span>
</div>

{/* With label */}
<div class="flex w-64 items-center gap-3">
  <Divider class="h-[2px] flex-1 bg-black" />
  <span class="text-xs font-medium text-[#6b7280]">OR</span>
  <Divider class="h-[2px] flex-1 bg-black" />
</div>
    ```
    </ForSolid>
  </Tabs.Tab>
</Tabs>

## Styling

Divider renders as a simple `<div>`. Style it with background color and dimensions based on orientation.

```tsx
{/* Horizontal */}
<Divider className="h-[2px] w-full bg-black" />

{/* Vertical */}
<Divider orientation="vertical" className="h-full w-[2px] bg-black" />
```

## Using data attributes

Divider does not expose interactive data attributes. When `decorative={false}`, it renders with `role="separator"` and `aria-orientation`.

```css
/* Style based on orientation */
[role="separator"][aria-orientation="vertical"] {
  width: 2px;
  height: 100%;
}
```

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | Divider direction |
| `decorative` | `boolean` | `true` | When `true`: `role="none"` + `aria-hidden`. When `false`: `role="separator"` + `aria-orientation` |
| `...rest` | `HTMLAttributes<HTMLDivElement>` | — | Spread onto the element |
