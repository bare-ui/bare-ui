---
title: Tooltip
description: Hover/focus tooltip with configurable delay and side.
---

import { Tabs } from 'nextra/components'


# Tooltip

Hover/focus tooltip. Configurable delay and side. Controlled and uncontrolled.

## Features

<FeatureList>
  <FeatureItem>Configurable delay before showing.</FeatureItem>
  <FeatureItem>Supports top, bottom, left, and right sides.</FeatureItem>
  <FeatureItem>Can be controlled or uncontrolled.</FeatureItem>
  <FeatureItem>Shows on hover and focus.</FeatureItem>
  <FeatureItem>data-state and data-side attributes for styling.</FeatureItem>
</FeatureList>

### Example

<Preview><TooltipPreview /></Preview>

<Tabs items={['Anatomy', 'Styled (Tailwind)']}>
  <Tabs.Tab>
    <ForReact>
    ```tsx
import { Tooltip } from '@wire-ui/react'

<Tooltip.Root delayDuration={0}>
  <Tooltip.Trigger>
    <button>Hover me</button>
  </Tooltip.Trigger>
  <Tooltip.Content side="top">
    Tooltip on top
  </Tooltip.Content>
</Tooltip.Root>
    ```
    </ForReact>
    <ForVue>
    ```vue
<script setup>
import { Tooltip, TooltipContent, TooltipRoot, TooltipTrigger } from '@wire-ui/vue'
</script>

<template>
    <TooltipRoot delayDuration={0}>
      <TooltipTrigger>
        <button>Hover me</button>
      </TooltipTrigger>
      <TooltipContent side="top">
        Tooltip on top
      </TooltipContent>
    </TooltipRoot>
</template>
    ```
    </ForVue>
    <ForSolid>
    🚧 Solid JS support is coming soon.
    </ForSolid>
  </Tabs.Tab>
  <Tabs.Tab>
    <ForReact>
```tsx
import { Tooltip } from '@wire-ui/react'

<Tooltip.Root delayDuration={0}>
  <Tooltip.Trigger>
    <button className="rounded-[8px] border-2 border-black bg-black px-4 py-2 text-sm font-medium text-white hover:bg-[#333]">
      Hover me
    </button>
  </Tooltip.Trigger>
  <Tooltip.Content
    side="top"
    className="rounded-[8px] border-2 border-black bg-[#f5f5f5] px-2.5 py-1.5 text-xs font-medium text-black data-[state=closed]:hidden whitespace-nowrap">
    Tooltip on top
  </Tooltip.Content>
</Tooltip.Root>
```
</ForReact>
<ForVue>
```vue
<script setup>
import { TooltipRoot, TooltipTrigger, TooltipContent } from '@wire-ui/vue'
</script>

<template>
  <TooltipRoot :delay-duration="0">
    <TooltipTrigger>
      <button class="rounded-[8px] border-2 border-black bg-black px-4 py-2 text-sm font-medium text-white hover:bg-[#333]">
        Hover me
      </button>
    </TooltipTrigger>
    <TooltipContent
      side="top"
      class="rounded-[8px] border-2 border-black bg-[#f5f5f5] px-2.5 py-1.5 text-xs font-medium text-black data-[state=closed]:hidden whitespace-nowrap">
      Tooltip on top
    </TooltipContent>
  </TooltipRoot>
</template>
```
</ForVue>
    <ForSolid>
    🚧 Solid JS support is coming soon.
    </ForSolid>
  </Tabs.Tab>
</Tabs>

## Styling

Tooltip Content receives `data-state` and `data-side`. Use them to control visibility and position-aware styling.

```tsx
<Tooltip.Content className="
  data-[state=closed]:hidden
  data-[side=top]:mb-2
  data-[side=bottom]:mt-2
  data-[side=left]:mr-2
  data-[side=right]:ml-2
" />
```

## Using data attributes

Content sets `data-state` (`"open"` / `"closed"`) and `data-side` (`"top"` / `"bottom"` / `"left"` / `"right"`).

```css
[data-state="closed"] { display: none; }
[data-side="top"]     { margin-bottom: 8px; }
[data-side="bottom"]  { margin-top: 8px; }
```

## Root props

| Prop | Type | Default | Description |
|---|---|---|---|
| `open` | `boolean` | — | Controlled open state |
| `defaultOpen` | `boolean` | `false` | Initial state for uncontrolled use |
| `onOpenChange` | `(open: boolean) => void` | — | Called when open state changes |
| `delayDuration` | `number` | `300` | Milliseconds before showing on hover |

## Content props

| Prop | Type | Default | Description |
|---|---|---|---|
| `side` | `'top' \| 'bottom' \| 'left' \| 'right'` | `'top'` | Preferred side to render |

## Content data attributes

| Attribute | Values |
|---|---|
| `data-state` | `"open"` / `"closed"` |
| `data-side` | `"top"` / `"bottom"` / `"left"` / `"right"` |

## Keyboard Interactions

| Key | Description |
|---|---|
| <kbd>Tab</kbd> | Shows the tooltip when trigger receives focus. |
| <kbd>Escape</kbd> | Closes the tooltip. |
