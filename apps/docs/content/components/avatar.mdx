---
title: Avatar
description: Image with a fallback for loading errors or missing sources.
---

import { Tabs } from 'nextra/components'


# Avatar

Image with a lazy-loading fallback. Fallback renders automatically when `src` is empty or the image fails to load.

## Features

<FeatureList>
  <FeatureItem>Automatic fallback on image load error or missing src.</FeatureItem>
  <FeatureItem>Fallback accepts any children (text, icons, etc.).</FeatureItem>
  <FeatureItem>data-status tracks loading, loaded, and error states.</FeatureItem>
  <FeatureItem>Lazy loading support.</FeatureItem>
</FeatureList>

### Example

<Preview><AvatarPreview /></Preview>

<Tabs items={['Anatomy', 'Styled (Tailwind)']}>
  <Tabs.Tab>
    <ForReact>
    ```tsx
import { Avatar } from '@wire-ui/react'

<div>
  <Avatar.Root>
    <Avatar.Image src="https://i.pravatar.cc/150?img=3" alt="Jane Doe" />
    <Avatar.Fallback>JD</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root>
    <Avatar.Fallback>AB</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root>
    <Avatar.Fallback>+9</Avatar.Fallback>
  </Avatar.Root>
</div>
    ```
    </ForReact>
    <ForVue>
    ```vue
<script setup>
import { Avatar, AvatarFallback, AvatarImage, AvatarRoot } from '@wire-ui/vue'
</script>

<template>
    <div>
      <AvatarRoot>
        <AvatarImage src="https://i.pravatar.cc/150?img=3" alt="Jane Doe" />
        <AvatarFallback>JD</AvatarFallback>
      </AvatarRoot>
      <AvatarRoot>
        <AvatarFallback>AB</AvatarFallback>
      </AvatarRoot>
      <AvatarRoot>
        <AvatarFallback>+9</AvatarFallback>
      </AvatarRoot>
    </div>
</template>
    ```
    </ForVue>
    <ForSolid>
    ```tsx
import { Avatar } from '@wire-ui/solid'

<div>
  <Avatar.Root>
    <Avatar.Image src="https://i.pravatar.cc/150?img=3" alt="Jane Doe" />
    <Avatar.Fallback>JD</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root>
    <Avatar.Fallback>AB</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root>
    <Avatar.Fallback>+9</Avatar.Fallback>
  </Avatar.Root>
</div>
    ```
    </ForSolid>
  </Tabs.Tab>
  <Tabs.Tab>
    <ForReact>
```tsx
import { Avatar } from '@wire-ui/react'

<div className="flex -space-x-2">
  <Avatar.Root className="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5] ring-2 ring-white">
    <Avatar.Image src="https://i.pravatar.cc/150?img=3" alt="Jane Doe" className="size-full object-cover" />
    <Avatar.Fallback className="text-sm font-medium text-black">JD</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root className="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5] ring-2 ring-white">
    <Avatar.Fallback className="text-sm font-medium text-black select-none">AB</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root className="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-black ring-2 ring-white">
    <Avatar.Fallback className="text-xs font-medium text-white">+9</Avatar.Fallback>
  </Avatar.Root>
</div>
```
</ForReact>
<ForVue>
```vue
<script setup>
import { AvatarRoot, AvatarImage, AvatarFallback } from '@wire-ui/vue'
</script>

<template>
  <div class="flex -space-x-2">
    <AvatarRoot class="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5] ring-2 ring-white">
      <AvatarImage src="https://i.pravatar.cc/150?img=3" alt="Jane Doe" class="size-full object-cover" />
      <AvatarFallback class="text-sm font-medium text-black">JD</AvatarFallback>
    </AvatarRoot>
    <AvatarRoot class="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5] ring-2 ring-white">
      <AvatarFallback class="text-sm font-medium text-black select-none">AB</AvatarFallback>
    </AvatarRoot>
    <AvatarRoot class="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-black ring-2 ring-white">
      <AvatarFallback class="text-xs font-medium text-white">+9</AvatarFallback>
    </AvatarRoot>
  </div>
</template>
```
</ForVue>
    <ForSolid>
```tsx
import { Avatar } from '@wire-ui/solid'

<div class="flex -space-x-2">
  <Avatar.Root class="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5] ring-2 ring-white">
    <Avatar.Image src="https://i.pravatar.cc/150?img=3" alt="Jane Doe" class="size-full object-cover" />
    <Avatar.Fallback class="text-sm font-medium text-black">JD</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root class="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5] ring-2 ring-white">
    <Avatar.Fallback class="text-sm font-medium text-black select-none">AB</Avatar.Fallback>
  </Avatar.Root>
  <Avatar.Root class="relative inline-flex size-10 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-black ring-2 ring-white">
    <Avatar.Fallback class="text-xs font-medium text-white">+9</Avatar.Fallback>
  </Avatar.Root>
</div>
```
</ForSolid>
  </Tabs.Tab>
</Tabs>

## Styling

Avatar Image exposes `data-status` to track the image loading lifecycle. Fallback renders automatically when status is `"error"` or `src` is empty.

```tsx
<Avatar.Root className="rounded-full overflow-hidden">
  <Avatar.Image className="
    size-full object-cover
    data-[status=loading]:opacity-0
    data-[status=loaded]:opacity-100
    transition-opacity
  " />
  <Avatar.Fallback className="text-sm font-medium">JD</Avatar.Fallback>
</Avatar.Root>
```

## Using data attributes

Avatar.Image sets `data-status` with values `"loading"`, `"loaded"`, or `"error"`.

```css
/* Hide image while loading */
[data-status="loading"] { opacity: 0; }
[data-status="loaded"]  { opacity: 1; }

/* Fallback renders when status is "error" or src is empty */
```

## Image data attributes

| Attribute | Values |
|---|---|
| `data-status` | `"loading"` → `"loaded"` / `"error"` |

`Fallback` renders when `data-status` is `"error"` or `src` is empty.

## Internals

Since `0.3.0`, the `Fallback` show-delay timer is delegated to <ForReact>[`useTimeout`](../hooks/use-timeout)</ForReact><ForVue>[`useTimeout`](../hooks/use-timeout)</ForVue><ForSolid>[`createTimeout`](../hooks/use-timeout)</ForSolid> instead of inline `setTimeout` bookkeeping. No public API change.
