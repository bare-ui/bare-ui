---
title: Card
description: Unstyled container with optional data-color and data-size attributes.
---

import { Tabs } from 'nextra/components'


# Card

A simple unstyled container element. Pass `data-color` and `data-size` to drive your design token styles.

## Features

<FeatureList>
  <FeatureItem>Unstyled container element.</FeatureItem>
  <FeatureItem>Supports color and size variants via data attributes.</FeatureItem>
  <FeatureItem>Composable with any child content.</FeatureItem>
</FeatureList>

### Example

<Preview><CardPreview /></Preview>

<Tabs items={['Anatomy', 'Styled (Tailwind)']}>
  <Tabs.Tab>
    <ForReact>
    ```tsx
import { Card, Avatar } from '@wire-ui/react'

<Card>
  <Avatar.Root>
    <Avatar.Fallback>W</Avatar.Fallback>
  </Avatar.Root>
  <div>
    <h3>Wire UI</h3>
    <p>Wireframe primitives for modern frameworks</p>
  </div>
</Card>
    ```
    </ForReact>
    <ForVue>
    ```vue
<script setup>
import { Avatar, AvatarFallback, AvatarRoot, Card } from '@wire-ui/vue'
</script>

<template>
    <Card>
      <AvatarRoot>
        <AvatarFallback>W</AvatarFallback>
      </AvatarRoot>
      <div>
        <h3>Wire UI</h3>
        <p>Wireframe primitives for modern frameworks</p>
      </div>
    </Card>
</template>
    ```
    </ForVue>
    <ForSolid>
    🚧 Solid JS support is coming soon.
    </ForSolid>
  </Tabs.Tab>
  <Tabs.Tab>
    <ForReact>
```tsx
import { Card, Avatar } from '@wire-ui/react'

<Card className="w-full max-w-sm rounded-[8px] border border-black bg-white p-4 flex items-center gap-4">
  <Avatar.Root className="relative inline-flex size-12 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5]">
    <Avatar.Fallback className="text-sm font-semibold text-black select-none">W</Avatar.Fallback>
  </Avatar.Root>
  <div>
    <h3 className="text-sm font-semibold text-black">Wire UI</h3>
    <p className="text-xs text-[#6b7280]">Wireframe primitives for modern frameworks</p>
  </div>
</Card>
```
</ForReact>
<ForVue>
```vue
<script setup>
import { Card, AvatarRoot, AvatarFallback } from '@wire-ui/vue'
</script>

<template>
  <Card class="w-full max-w-sm rounded-[8px] border border-black bg-white p-4 flex items-center gap-4">
    <AvatarRoot class="relative inline-flex size-12 shrink-0 items-center justify-center overflow-hidden rounded-full border border-black bg-[#f5f5f5]">
      <AvatarFallback class="text-sm font-semibold text-black select-none">W</AvatarFallback>
    </AvatarRoot>
    <div>
      <h3 class="text-sm font-semibold text-black">Wire UI</h3>
      <p class="text-xs text-[#6b7280]">Wireframe primitives for modern frameworks</p>
    </div>
  </Card>
</template>
```
</ForVue>
    <ForSolid>
    🚧 Solid JS support is coming soon.
    </ForSolid>
  </Tabs.Tab>
</Tabs>

## Styling

Card passes `data-color` and `data-size` as standard HTML attributes. Use them as CSS hooks for your design tokens.

```tsx
<Card color="primary" size="large" className="
  [data-color=primary]:bg-gray-50
  [data-color=inverse]:bg-black [data-color=inverse]:text-white
  [data-size=small]:p-3
  [data-size=large]:p-6
" />
```

## Using data attributes

Card sets `data-color` and `data-size` from their respective props.

```css
[data-color="primary"] { background: #f5f5f5; }
[data-color="inverse"] { background: #000; color: #fff; }
[data-size="small"]    { padding: 0.75rem; }
[data-size="large"]    { padding: 1.5rem; }
```

## Props

All standard `HTMLAttributes<HTMLDivElement>` — `className`, `onClick`, etc. `data-color` and `data-size` are passed through as standard HTML attributes and used as CSS hooks.

## Styling with data attributes

```css
[data-color="blue"]  { background: var(--color-blue-50); }
[data-color="green"] { background: var(--color-green-50); }
[data-size="small"]  { padding: var(--space-2); }
[data-size="large"]  { padding: var(--space-6); }
```
